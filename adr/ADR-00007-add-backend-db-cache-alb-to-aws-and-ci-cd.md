# ADR-00007: Making the backend, db, and cache live on AWS.

## Status
Accepted

## Summary
Set up the backend (ECS container), database (RDS), and Redis cache (ElastiCache) on AWS.

## Decision
